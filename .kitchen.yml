---
driver:
  name: vagrant

provisioner:
  name: chef_zero

platforms:
  - name: ubuntu-12.04
  - name: debian-7.4

suites:
  - name: duply
    run_list:
      - recipe[apt::default]
      - recipe[fake::default]
    attributes:
